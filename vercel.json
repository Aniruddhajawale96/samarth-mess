{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install && pnpm --filter @samarth-mess/types build && pnpm --filter @samarth-mess/shared build && pnpm --filter @samarth-mess/validation build && pnpm --filter @samarth-mess/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "rewrites": [
    {
      "source": "/api/proxy/:path*",
      "destination": "/api/proxy/:path*"
    }
  ]
}
