{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @samarth-mess/types build && pnpm --filter @samarth-mess/shared build && pnpm --filter @samarth-mess/validation build && pnpm --filter @samarth-mess/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "corepack enable && pnpm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "rewrites": [
    {
      "source": "/api/proxy/:path*",
      "destination": "/api/proxy/:path*"
    }
  ]
}
